# Copy this file to ".env" and fill in your key.
# The .env file is git-ignored and is read only by server.js — it is never
# sent to the browser.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: change the port the local server listens on (default 8000).
# PORT=8000
